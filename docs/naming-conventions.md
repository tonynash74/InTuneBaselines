# Naming conventions for Cyber Essentials baselines

This repo uses **consistent names** so engineers and auditors can see what each policy does at a glance.

## Display name pattern (Intune)

For all baseline policies created from this repo:

```text
CE-<Level> <Platform> <Scope> – <Type> (<VersionTag>)
