## T12 – CI pipeline & basic tests

### `.github/workflows/validate-baselines.yml`

#GitHub Actions workflow to:

#- Run **Pester** tests on JSON baselines.
#- Run **PSScriptAnalyzer** on `scripts/*.ps1`.

```yaml
name: Validate baselines

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PowerShell
        uses: PowerShell/PowerShell-action@v2
        with:
          pwsh-version: 'latest'

      - name: Install test modules
        run: |
          pwsh -Command '
            Install-Module Pester -Scope CurrentUser -Force -MinimumVersion 5.0.0;
            Install-Module PSScriptAnalyzer -Scope CurrentUser -Force
          '

      - name: Run PSScriptAnalyzer on scripts
        run: |
          pwsh -Command '
            Import-Module PSScriptAnalyzer;
            if (Test-Path "./scripts") {
              Invoke-ScriptAnalyzer -Path "./scripts" -Recurse -Severity Warning,Error -ExcludeRule PSAvoidUsingWriteHost;
            } else {
              Write-Host "No scripts directory found, skipping ScriptAnalyzer.";
            }
          '

      - name: Run Pester tests
        run: |
          pwsh -Command '
            Invoke-Pester -Path "./tests" -Output Detailed
          '
